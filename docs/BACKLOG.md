# Backlog

## UI / Usability
- [ ] **Focus after +Item**: When creating a new item via "+Item", the selection highlights the item, but keyboard focus remains on the button (or window), so hitting 'Delete' immediately does not work. User must click the TrackView to focus before deleting.

## Core Features
- [x] **Track Deletion**: Ability to delete selected tracks.
